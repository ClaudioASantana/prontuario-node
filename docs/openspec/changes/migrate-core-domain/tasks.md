# Tasks

- [ ] Enhance `Auth` module for RBAC (Roles: Patient, Physician, Admin, Plan)
- [ ] Scaffold `HealthPlan` module (Convênios)
- [ ] Implement `HealthPlan` entities and logic
- [ ] Scaffold `Patient` module in Backend `nest g module modules/patients`
- [ ] Define `Patient` Domain Entity (Profile only, separate from Records)
- [ ] Implement `Patient` TypeORM Entity and Repository
- [ ] Create `Patient` Service and Controller
- [ ] Scaffold `Physician` module (separate from Record logic)
- [ ] Implement `Physician` entities and logic
- [ ] Scaffold `MedicalRecord` module (Core)
- [ ] Implement `MedicalRecord` entities with Audit Logging (ResumoIA requirement)
- [ ] Verify RBAC and Audit flows
